---
# yamllint disable rule:line-length
name: porter-test
version: 0.1.0
description: "Permissions test"
registry: crateanon

mixins:
  - exec

install:
  - exec:
      command: ./wrong_permission.sh

uninstall:
  - exec:
      command: ./wrong_permission.sh
